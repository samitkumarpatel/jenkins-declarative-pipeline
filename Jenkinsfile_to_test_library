library identifier: 'jenkins-shared-libraries@question/stackoverflow', retriever: modernSCM([
        $class: 'GitSCMSource',
        remote: 'https://github.com/samitkumarpatel/jenkins-shared-libs.git'
]) net.samittutorial.Math

pipeline {
    agent any
    stages {
        stage('debug') {
            steps {
                echo "Hello"
                sh "ls -al"
                info()
            }
        }
    }
}
