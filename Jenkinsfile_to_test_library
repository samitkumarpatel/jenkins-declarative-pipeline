library(
        identifier: 'jenkins-shared-libraries@question/stackoverflow', 
            retriever: modernSCM([
                $class: 'GitSCMSource',
                remote: 'https://github.com/samitkumarpatel/jenkins-shared-libs.git'
            ])
    )
pipeline {
    agent any
    stages {
        stage('debug') {
            steps {
                echo "Hello"
                library('jenkins-shared-libraries').net.samittutorial.Math.info()
            }
        }
    }
}
