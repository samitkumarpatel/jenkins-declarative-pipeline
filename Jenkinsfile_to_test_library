
library identifier: 'jenkins-shared-libraries@question/stackoverflow', 
    retriever: modernSCM([
        $class: 'GitSCMSource',
        remote: 'https://github.com/samitkumarpatel/jenkins-shared-libs.git'
    ])

import net.samittutorial.Math    

pipeline {
    agent any
    stages {
        stage('debug') {
            steps {
                echo "Hello"
                
                script {
                    def math = new Math(this)
                    println math.add(2,3)
                }
            }
        }
    }
}
