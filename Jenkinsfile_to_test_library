def l = library(
        identifier: 'jenkins-shared-libraries@question/stackoverflow', 
            retriever: modernSCM([
                $class: 'GitSCMSource',
                remote: 'https://github.com/samitkumarpatel/jenkins-shared-libs.git'
            ])
    )
pipeline {
    agent any
    stages {
        stage('debug') {
            steps {
                echo "Hello"
                l.net.samittutorial.Math.info()
                println math.add(2,3)
            }
        }
    }
}
